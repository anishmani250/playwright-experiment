{
  "name": "playwright-gh-user-search-final-api-notoken",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:pages": "playwright test --grep @pages",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "typescript": "^5.4.0"
  }
}
